{
  "name": "src",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "check:types": "pnpm -r exec tsc --noEmit",
    "clean": "rimraf coverage build tmp",
    "test": "vitest",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "22.14.0",
    "esbuild": "0.25.2",
    "tsutils": "3.21.0",
    "typescript": "5.8.3",
    "vite": "6.2.5",
    "vitest": "3.1.1"
  }
}
