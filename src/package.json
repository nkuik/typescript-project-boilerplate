{
  "name": "src",
  "version": "0.0.1",
  "description": "Boilerplate TS project",
  "scripts": {
    "build": "tsc",
    "check:types": "pnpm -r exec tsc --noEmit",
    "clean": "rimraf coverage build tmp",
    "test": "vitest",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "22.13.12",
    "esbuild": "0.25.1",
    "tsutils": "3.21.0",
    "typescript": "5.8.2",
    "vite": "6.2.2",
    "vitest": "3.0.9"
  }
}
