{
  "name": "src",
  "version": "0.0.1",
  "description": "Boilerplate TS project",
  "scripts": {
    "build": "tsc",
    "check:types": "pnpm -r exec tsc --noEmit",
    "clean": "rimraf coverage build tmp",
    "test": "vitest",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "22.10.6",
    "esbuild": "0.24.2",
    "tsutils": "3.21.0",
    "typescript": "5.7.3",
    "vite": "6.0.7",
    "vitest": "2.1.8"
  }
}
